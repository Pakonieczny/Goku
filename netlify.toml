[build]
  command = "npm run build"
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["form-data", "node-fetch", "formidable"]

[[functions]]
  name = "imageUpload"
  rawBody = true
